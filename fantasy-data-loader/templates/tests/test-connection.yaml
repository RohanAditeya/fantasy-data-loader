apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fantasy-data-loader.fullname" . }}-test-connection"
  labels:
    {{- include "fantasy-data-loader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fantasy-data-loader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
